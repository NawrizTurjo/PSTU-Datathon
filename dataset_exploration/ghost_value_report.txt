=== Ghost value hunt: searching every numeric column for exact -999999 ===
  base_number_of_dependent_farmers: train=66 (0.137%)  test=23 (0.191%)

CONFIRMED single ghost marker: -999999 appears ONLY in 'base_number_of_dependent_farmers'.
Failure rate when ghosted: 0.0303 vs normal rows: 0.0500
Physically impossible: a negative dependent-farmer count.
Recommendation: replace with NaN, then impute (median/mode) or let a tree model (LightGBM/XGBoost/CatBoost) split on it natively - it's rare (~0.15% of rows) so impact is small either way.

=== Secondary note: heavy-tailed sensor columns (NOT the same as the ghost marker above) ===
sensor_average_daily_pump_runtime_hours, sensor_short_term_pump_runtime_hours,
sensor_long_term_pump_runtime_hours, sensor_wind_speed_kmh,
sensor_daily_water_demand_liters show many repeated round-number outliers
(90000, 150000, 300000, ...) far beyond physical plausibility (e.g. hours/day > 24).
These look like a multiplicative noise/scaling artifact rather than one sentinel value.
Recommendation: cap/winsorize at a reasonable percentile (e.g. p99.5) or log1p-transform;
tree models are fairly robust to this but linear/distance-based models are not.
